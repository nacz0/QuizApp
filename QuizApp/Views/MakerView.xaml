<UserControl x:Class="QuizApp.Views.MakerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel Margin="10">
        <TextBox Text="{Binding QuizName, UpdateSourceTrigger=PropertyChanged}" 
              Margin="0,0,0,10" />

        <ListBox ItemsSource="{Binding Questions}" 
             SelectedItem="{Binding SelectedQuestion}" 
             DisplayMemberPath="Text" 
             Margin="0,0,0,10" />

        <TextBox Text="{Binding QuestionText, UpdateSourceTrigger=PropertyChanged}" 
              Margin="0,0,0,10" />

        <ItemsControl ItemsSource="{Binding Answers}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Width="200" />
                        <CheckBox IsChecked="{Binding IsCorrect}" Content="Poprawna" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Content="Dodaj odpowiedź" Command="{Binding AddAnswerCommand}" Margin="0,10,0,0" />
        <Button Content="Dodaj pytanie" Command="{Binding NextQuestionCommand}" Margin="0,10,0,0" />
        <Button Content="Edytuj pytanie" Command="{Binding EditQuestionCommand}" Margin="0,10,0,0" />
        <Button Content="Usuń pytanie" Command="{Binding DeleteQuestionCommand}" Margin="0,10,0,0" />
        <Button Content="Zakończ quiz" Command="{Binding FinishQuizCommand}" Margin="0,10,0,0" />
        <Button Content="Wczytaj quiz" Command="{Binding LoadQuizCommand}" Margin="0,10,0,0" />
    </StackPanel>


</UserControl>
